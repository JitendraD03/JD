#include <stdio.h>
void display(int arr[], int n)
{
    printf("Array elements--->\n");
    for (int i = 0; i < n; i++)
    {
        printf("%d  ", arr[i]);
    }
    printf("\n");
}
int delete(int arr[], int n, int x)
{
    int i;
    for (i = 0; i < n; i++) // main deletion code
    {
        if (arr[i] == x) // element mil gya bahar aaja
        {
            break;
        }
    }
    if (i == n)
        return n;
    for (int j = i; j < n - 1; j++) // after delete fill the empty space
    {
        arr[j] = arr[j + 1];
    }
    return n - 1; // size minus 1 because one element is deleted
}
int main()
{
    int arr[10] = {5,8,4,3,9};
    int n=5;
    int x=8;
    display(arr,n);
    n=delete(arr,n,x);
    display(arr,n);
    return 0;
}
